<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="csrf-token" content={csrf_token_value()}>
    <%= live_title_tag assigns[:page_title] || "Onagal", suffix: " · Phoenix Framework" %>
    <link phx-track-static rel="stylesheet" href={Routes.static_path(@conn, "/assets/app.css")}/>
    <script defer phx-track-static type="text/javascript" src={Routes.static_path(@conn, "/assets/app.js")}></script>
  </head>
  <body>
    <header>
      <div class="flex-container" style="display: flex">
        <div class="flex-child" style="flex: 1; background: #3199aa">
          <table>
            <tr>
              <td><%= link "Images", to: Routes.image_index_path(@conn, :index) %></td>
              <td><%= link "Tags", to: Routes.tag_index_path(@conn, :index) %></td>
              <td><%= link "Tagsets", to: Routes.tagset_index_path(@conn, :index) %></td>
              <td><%= link "Galleries", to: Routes.gallery_index_path(@conn, :index) %></td>
            </tr>
          </table>
        </div>
        <div class="flex-child" style="flex: 0 1 240px; background: #aa7724">
          <%= render "_user_menu.html", assigns %>
        </div>
      </div>
    </header>
    <div class="flex-container" style="display: flex; align-items: stretch;">
      <div class="flex-child" style="flex: 0 1 240px; background: #03a9f4;">
        <%= if @live_action == :index and @live_module == OnagalWeb.ImageLive.Index do %>
          <%= render "_filter_form.html", assigns %>
        <% end %>
      </div>
      <div class="flex-child" style="flex: 1; height: 720px;">
        <%= @inner_content %>
      </div>
    </div>
  </body>
</html>
